# webview_all_linux example

Run from this directory:

```bash
flutter pub get
flutter run -d linux
```

This example depends on the local `webview_all` package and overrides
`webview_all_linux` to the parent directory so Linux changes can be tested
without publishing.
